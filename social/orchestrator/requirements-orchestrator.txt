# Social Ads Orchestrator - Unified Dependencies
# Includes all dependencies for Microsoft, LinkedIn, Facebook, and Google platforms
# Python 3.12+

# Core dependencies
requests==2.32.3
pandas==2.2.3
loguru==0.7.3
pyyaml==6.0.2

# Microsoft Ads (BingAds SDK)
bingads==13.0.24.2

# Facebook Ads (Graph API SDK)
facebook-business==19.0.3

# Google Ads (gRPC + Protobuf)
google-ads==24.1.0

# LinkedIn Ads (custom HTTP client, no SDK needed)
# Uses requests (already included above)

# Database connectors
vertica-python==1.4.0

# Azure storage (optional)
azure-data-tables==12.5.0
azure-storage-blob==12.19.0

# Orchestrator-specific
schedule==1.2.0  # For cron-like scheduling
tenacity==8.2.3  # For retry logic with exponential backoff

# Type checking (development)
mypy==1.13.0
types-requests==2.32.0.20240914
types-PyYAML==6.0.12.20240917
